import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ApplyCardPage } from './apply-card';

@NgModule({
  declarations: [
    ApplyCardPage,
  ],
  imports: [
    IonicPageModule.forChild(ApplyCardPage),
  ],
})
export class ApplyCardPageModule {}
